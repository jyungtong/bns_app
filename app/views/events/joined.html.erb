<h1>Joined Events</h1>

<div id="events">
	<% unless @events.empty? %>
    <%= render "events/student/event_table" %>
	<% else %>
		<%= content_tag :p, "You have not joined any events yet." %>
	<% end %>
</div>
