<%= link_to "B&S Home", root_path, :class => 'brand' %>
<ul class="nav">
	<% if student_signed_in? %>
		<%= render 'layouts/student_actions' %>
	<% elsif admin_signed_in? %>
		<%= render 'layouts/admin_actions' %>
	<% else %>
		<%= render 'layouts/anonymous_actions' %>
	<% end %>
</ul>
<ul class="nav pull-right">
  <li class="navbar-text"><%= greet_message %></li>
</ul>
